<p id="notice"><%= notice %></p>

<p>
  <strong>Nombre:</strong>
  <%= @employee.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @employee.email %>
</p>
<p>
  <strong>Habilitado:</strong>
  <%= @employee.lock %>
</p>

<%= link_to 'Editar', edit_employee_path(@employee), class: 'btn btn-primary' %>
<%= link_to 'Volver', employees_path, class: 'btn btn-default' %>
