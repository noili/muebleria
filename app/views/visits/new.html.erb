<%= form_for [:client, @visit] do |f| %>
  <div>
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>
  <div>
    <%= f.label :duration %>
    <%= f.text_field :duration %>
  </div>

  <%= f.submit "Guardar" %>
<% end %>
